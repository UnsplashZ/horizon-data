{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "覆盖窗口的默认能力",
  "windows": ["main", "inputs", "grip", "gforce", "settings"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default",
    "global-shortcut:default"
  ]
}
