# 待办 / TODO

当前没有待办项。下面是关键里程碑记录（节选）。

## 已完成（节选）

- 多窗口拆分、独立定位/持久化、缩放（角柄）、透明度拆分、换挡灯、输入/抓地/G力/胎温模块、圆角修复。
- **胎温改为摄氏度** —— 华氏度转摄氏度，同步更新颜色阈值和显示单位（2026-06-16）。
- **编辑模式窗口拖动** —— pointerdown 中同步发起 `startDragging`/`startResizeDragging`，Windows 和 macOS 均可拖动/缩放（2026-06-16）。
- **清理旧独立模块视图与旧设置窗口** —— 当前生产代码只保留主 HUD（`main`）与托盘弹出控制面板（`controls`），输入/轮胎/G力模块集成在主 HUD 中；旧的「各窗口统一高度」方案随之废弃（2026-06-17）。
- **托盘控制入口** —— 左键打开/隐藏控制面板，支持端口直接输入、透明度滑条、单位、模块开关、编辑/锁定和退出（2026-06-17）。
- **HUD 显示/隐藏切换** —— 托盘菜单与对应逻辑（2026-06-18）。
- **运行时配置失败提示** —— UDP 绑定、快捷键注册等失败状态回传前端展示（2026-06-18）。
- **控制面板关闭按钮** —— 标题栏右上角 `×` 仅隐藏面板，不改动编辑/锁定状态（2026-06-24）。
- **多段换挡提示** —— 转速分接近/换挡/触顶三段，文字分色（琥珀/橙/红）+ 分级闪烁；FH6 无独立红线字段，按占 `EngineMaxRpm` 比例近似（2026-06-24）。
